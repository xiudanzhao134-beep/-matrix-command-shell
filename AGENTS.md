# 项目长期规则

- 本仓库是公开 GitHub Pages 仓库。
- 只允许提交网页程序代码。
- 禁止提交任何公司真实账号名、播放量、涨粉、获客、模板图片数据包、私人 JSON、Excel 数据、Cookie、Token、账号密码或其他公司经营数据。
- 私人数据必须继续通过浏览器本地导入。
- 修改 `index.html` 前先检查现有结构。
- 修改完成后检查 `git diff`。
- 只提交必要文件。
- 默认提交到 `main`。
- push 到 `origin/main`。
- 不允许 force push。
